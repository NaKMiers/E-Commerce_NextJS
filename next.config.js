/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['firebasestorage.googleapis.com', 'googleapis.com', 'lh3.googleusercontent.com'],
  },
}

module.exports = nextConfig
